<%= simple_form_for @usuario, :validate => true do |f| %>
<div class="row">
	<div class="col-md-4">
		<div class="field">
			<%= f.input :nombre, :input_html => {class: "form-control"} %>
			<%= f.input :email, :input_html => {class: "form-control"}  %>
		</div>
	</div>
	<div class="col-md-4">
		<div class="field">
			<%= f.input :password, :input_html => {class: "form-control"} %>
			<%= f.input :password_confirmation, :input_html => {class: "form-control"} %>
		</div>
	</div>
</div>
</br>
	<div class="col-md-offset-9">
		<div class="actions" id="botones_formulario" >
			<% if controller.action_name == 'new' or controller.action_name == 'index' %>
				<%= f.submit "Agregar", :class => 'mui-btn mui-btn-primary mui-btn-raised'%>
			<% elsif controller.action_name == 'edit' %>
				<%= f.submit "Actualizar", :class => 'mui-btn mui-btn-primary mui-btn-raised'%>
			<% end %>
			<%= button_tag "Cancelar", type: :reset , :class => 'mui-btn mui-btn-raised mui-btn-default'%>
		</div>
	</div>
<% end %>